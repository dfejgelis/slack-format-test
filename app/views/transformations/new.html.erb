<%= form_for :transformation, url: transformations_path do |f| %>

  <% if @transformation.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@transformation.errors.count, "error") %> prohibited
        this transformation from being saved:
      </h2>
      <ul>
        <% @transformation.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>


<%= link_to 'Back', transformations_path %>
